-  you can write any decimal number using positional table
-  this is the same for binary system as well (base 8), but you use 2 to the power of something
-  remember the values of the powers in the binary system so you only have to do simple addition from the table, not division
-  1 byte = 8 bits
-  hexadecimal system (base 16) - if we have 2A in hexadecimal and we want to convert to decimal? - convert to binary then convert to decimal

